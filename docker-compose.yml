version: '3'

networks:
  mo_net:
    name: mo_net
    external: true
    
services:
  kms_mo:
    image: mikolatero/vlmcsd
    container_name: kms_mo
    restart: always
    networks:
      - mo_net
    ports:
      - 1688:1688
